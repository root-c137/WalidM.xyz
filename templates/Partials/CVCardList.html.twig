
<div class="CVList">

{% for Cv in CVList %}
    <div class="CvCard">

        <div class="Infos">
            <div class="Title">{{ Cv.Title }}</div>
            <div class="Company">{{ Cv.Company }}</div>
            <div class="Description">{{ Cv.Description }}</div>
        </div>

        <div class="Date">
                {{ Cv.StartDate }}<span class="Fleche">→</span>{{ Cv.EndDate }}
        </div>

    </div>
{% endfor %}
</div>


